import 'package:momenter/src/moment.dart';

abstract class FakeMoments {
  static final oneSecond = Moment(const Duration(seconds: 1));
  static final twoSeconds = Moment(const Duration(seconds: 2));
  static final threeSeconds = Moment(const Duration(seconds: 3));
  static final fourSeconds = Moment(const Duration(seconds: 4));
  static final fiveSeconds = Moment(const Duration(seconds: 5));
  static final sixSeconds = Moment(const Duration(seconds: 6));
  static final sevenSeconds = Moment(const Duration(seconds: 7));
  static final eightSeconds = Moment(const Duration(seconds: 8));
  static final nineSeconds = Moment(const Duration(seconds: 9));
  static final tenSeconds = Moment(const Duration(seconds: 10));
}
